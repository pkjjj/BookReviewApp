import { UserForRegistration } from './user-for-registration.model';

describe('UserForRegistration', () => {
  it('should create an instance', () => {
    expect(new UserForRegistration()).toBeTruthy();
  });
});
